# General configuration for the init.d scripts,
# not necessarily for JBoss AS itself.
# default location: /etc/default/wildfly

# Location of JDK
#
JAVA_HOME="<%= scope.lookupvar('wildfly::java_home') %>"

# Location of WildFly
#
JBOSS_HOME="<%= scope.lookupvar('wildfly::dirname') %>"

# The username who should own the process.
#
JBOSS_USER="<%= scope.lookupvar('wildfly::user') %>"

# The mode WildFly should start, standalone or domain
#
JBOSS_MODE="<%= scope.lookupvar('wildfly::mode') %>"

# Configuration for standalone mode
#
JBOSS_CONFIG="<%= scope.lookupvar('wildfly::config') %> -P <%= scope.lookupvar('wildfly::dirname') %>/jboss.properties"

# Configuration for domain mode
#
JBOSS_DOMAIN_CONFIG="<%= scope.lookupvar('wildfly::domain_config') %>"
JBOSS_HOST_CONFIG="<%= scope.lookupvar('wildfly::host_config') %> -P <%= scope.lookupvar('wildfly::dirname') %>/jboss.properties"

# The amount of time to wait for startup
#
STARTUP_WAIT="<%= scope.lookupvar('wildfly::startup_wait') %>"

# The amount of time to wait for shutdown
#
SHUTDOWN_WAIT="<%= scope.lookupvar('wildfly::shutdown_wait') %>"

# Location to keep the console log
#
JBOSS_CONSOLE_LOG="<%= scope.lookupvar('wildfly::console_log') %>"

# Startup script options
#
JBOSS_OPTS="<%= scope.lookupvar('wildfly::jboss_opts') %>"
