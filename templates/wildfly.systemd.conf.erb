<% config = scope.lookupvar('wildfly::mode') == 'domain'? scope.lookupvar('wildfly::host_config') : scope.lookupvar('wildfly::config') %>

# The configuration you want to run
WILDFLY_CONFIG="'<%= config.to_s %> -P <%= scope.lookupvar('wildfly::dirname') %>/jboss.properties'"

# The mode you want to run
WILDFLY_MODE=<%= scope.lookupvar('wildfly::mode') %>

# The address to bind to
WILDFLY_BIND=<%= scope.lookupvar('wildfly::properties')['jboss.bind.address'] %>

# Location of JDK
JAVA_HOME=<%= scope.lookupvar('wildfly::java_home') %>
