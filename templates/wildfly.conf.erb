# General configuration for the init.d scripts,
# not necessarily for JBoss AS itself.
# default location: /etc/default/wildfly

# Location of JDK	
#
JAVA_HOME=<%= scope['wildfly::java_home'] %>

# Location of WildFly
#
JBOSS_HOME=<%= scope['wildfly::dirname'] %>

# The username who should own the process.
#
JBOSS_USER=<%= scope['wildfly::user'] %>

# The mode WildFly should start, standalone or domain
#
JBOSS_MODE=<%= scope['wildfly::mode'] %>

# Configuration for standalone mode
#
JBOSS_CONFIG=<%= scope['wildfly::config'] %>

# Configuration for domain mode
#
JBOSS_DOMAIN_CONFIG=<%= scope['wildfly::domain_config'] %>
JBOSS_HOST_CONFIG=<%= scope['wildfly::host_config'] %>

# The amount of time to wait for startup
#
STARTUP_WAIT=30

# The amount of time to wait for shutdown
#
SHUTDOWN_WAIT=30

# Location to keep the console log
#
JBOSS_CONSOLE_LOG=<%= scope['wildfly::console_log'] %>

